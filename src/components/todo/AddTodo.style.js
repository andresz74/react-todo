import { style as s } from 'typestyle';

export const addForm = s({
	display: 'flex',
});

export const addItemEntry = s({
    border: 0,
    borderBottom: '1px #555 solid',
    borderRadius: 0,
	display: 'flex',
    flexGrow: 1,
    padding: '10px',
});

export const addButton = s({
    backgroundColor: '#555',
    border: '1px #555 solid',
    color: '#fff',
    display: 'flex',
    padding: '10px 25px',
});
