import { style as s } from 'typestyle';

export const headerStyle = s({
	alignItems: 'center',
	backgroundColor: '#333',
	color: '#fff',
	display: 'flex',
	flexDirection: 'column',
	justifyContent: 'center',
	padding: '15px 0',
});

export const titleStyle = s({
	display: 'block',
});

export const menuStyle = s({
	color: '#fff',
	display: 'flex',
	flexDirection: 'row',
});

export const linkStyle = s({
	color: '#fff',
	marginRight: '15px',
})
